# IDE / OS
.DS_Store
Thumbs.db
/.idea/
/.vscode/
/.zed/
/.nova/
/.fleet/

# El subproyecto mobile/ tiene su propio .gitignore (Flutter default + ajustes).
# Las únicas reglas root quedan acá.

.env
.env.tailscale
*.ts.net.crt
*.ts.net.key
.phpunit.result.cache
.playwright-mcp/

# `adb backup io.github.gregori100.fincore` deja un .ab vacío (47 bytes) en cwd.
# Es el artefacto del smoke de privacidad (RF-008); no se commitea.
*.ab

# Worktrees de git creados por Claude Code (no son código fuente).
.claude/worktrees/
